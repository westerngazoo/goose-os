[package]
name = "goose-os"
version = "0.1.0"
edition = "2021"

[features]
default = ["qemu"]
qemu = []
vf2 = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true

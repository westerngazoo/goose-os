# Scripts — device-side tooling

Shell scripts that run **on** the target hardware (VF2 Debian rootfs
used as recovery/bringup OS) or on the developer host.

Phase 0 scaffold — population by phase:
  - `goose-upgrade.sh` equivalent, ported from goose-os — Phase 1a
  - `flash-sd.sh` — Phase 1b
